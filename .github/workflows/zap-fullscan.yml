name: OWASP ZAP Full Scan

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  zap_scan:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout código
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # ----------------------------
      # 🔧 FIX PARA PERMISOS DE ZAP
      # ----------------------------
      - name: 🔓 Dar permisos absolutos al workspace
        run: sudo chmod -R 777 ${{ github.workspace }}

      # Crear red dedicada para backend + mongo
      - name: 🌐 Crear red compartida
        run: docker network create zapnet || true

      # MongoDB en la MISMA red que el backend
      - name: 🐳 Levantar MongoDB en Docker
        run: |
          docker run -d \
            --name mongo \
            --network zapnet \
            -p 27017:27017 \
            mongo:7

      # Build imagen backend
      - name: 🏗️ Build imagen backend
        run: docker build -t backend-app ./backend

      # Levantar backend en Docker
      - name: 🚀 Levantar backend en Docker
        run: |
          docker run -d \
            --name backend-app \
            --network zapnet \
            -p 8080:8080 \
            -e NODE_ENV=production \
            -e DOCKER_ENV=true \
            -e USE_MEMORY_DB=false \
            -e DB_NAME=test \
            -e MONGO_URL=mongodb://mongo:27017 \
            -e MONGO_URL_LOCAL=mongodb://mongo:27017 \
            -e JWT_SECRET=test_secret \
            -e FRONTEND_URL=http://localhost:3000 \
            backend-app

      # Esperar backend
      - name: ⏳ Esperar backend
        run: |
          echo "Esperando que el backend responda..."
          for i in {1..60}; do
            if curl -sSf http://localhost:8080/health > /dev/null; then
              echo "✅ Backend listo en intento $i"
              exit 0
            fi
            echo "⏳ Intento $i... backend no disponible"
            sleep 5
          done
          echo "❌ Backend no inició a tiempo"
          docker logs backend-app
          exit 1

      # ----------------------------
      # 🔧 FIX PARA ESCRITURA DE REPORTES
      # ----------------------------
      - name: 🔓 Permitir escritura en /zap/wrk
        run: sudo chmod -R 777 .

      # Ejecutar ZAP Full Scan
      - name: 🕷️ Ejecutar OWASP ZAP Full Scan
        uses: zaproxy/action-full-scan@v0.10.0
        with:
          target: "http://localhost:8080"
          cmd_options: >
            -a
            -T 600
            --allow-scripts
            --hook=/zap/wrk
            -r zap_report.html
            -J zap_report.json
            -w zap_report.md

      # Guardar reportes
      - name: 💾 Guardar reportes
        uses: actions/upload-artifact@v4
        with:
          name: zap_reports
          path: |
            zap_report.html
            zap_report.json
            zap_report.md

      # Limpieza
      - name: 🧹 Limpieza
        if: always()
        run: |
          docker logs backend-app || true
          docker rm -f backend-app || true
          docker rm -f mongo || true
          docker network rm zapnet || true















